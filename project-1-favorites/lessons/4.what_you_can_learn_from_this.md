USE PDA account to store user's favorites. 

Things you need to pay attention
1) since user may have multiple hobbies , you should use dynamic variable like vector to store them.

Q1: so the question is : 
what's the size limitation for  a PDA account to store data on chain?

A1: The maximum data size for any Solana account (including a PDA account) is 10 MiB.1

From the documented Account type: its data field (the on-chain data buffer) has a maximum size of 10 MiB.

